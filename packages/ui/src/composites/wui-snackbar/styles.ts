import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 40,
    flexDirection: 'row',
    paddingHorizontal: 8,
    borderRadius: 100,
    borderWidth: 1,
    alignItems: 'center'
  },
  text: {
    paddingHorizontal: 8
  }
});

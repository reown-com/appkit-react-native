import { AccountPlaceholder } from '../w3m-account-placeholder';

export function AccountTokens() {
  return (
    <AccountPlaceholder
      icon="wallet"
      title="No token"
      description="Buy crypto or receive funds to trade"
    />
  );
}
